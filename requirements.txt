# ARUNA AI - runtime dependencies (PHASE 1)
# Install:  .venv\Scripts\python.exe -m pip install -r requirements.txt

pydantic>=2.9
pydantic-settings>=2.6

# MySQL driver. ARUNA runs against Laragon's bundled MySQL 8.4; the schema
# uses CHECK constraints and JSON, so MySQL 8.0.16+ is the floor.
asyncmy>=0.2.9

redis>=5.2
structlog>=24.4
python-telegram-bot>=21.9

# Imported directly by the provider adapters. It also arrives transitively via
# python-telegram-bot, but depending on that would leave ARUNA broken with no
# declared reason if ptb ever swapped its HTTP client.
httpx>=0.27

# Windows does not ship the IANA timezone database; ZoneInfo("Asia/Jakarta")
# needed by the IDX session clock fails without this.
tzdata>=2024.2
